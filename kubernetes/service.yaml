apiVersion: v1
kind: Service
metadata:
  name: attention-service
  labels:
    app: attention
spec:
  type: NodePort
  selector:
    app: attention
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 30080
